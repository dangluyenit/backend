'use strict';

class Lesson {
  constructor(id, name, content, image, video, idCourse) {
    this.id = id;
    this.name = name;
    this.content = content;
    this.image = image;
    this.video = video;
    this.idCourse = idCourse;
  }
}
